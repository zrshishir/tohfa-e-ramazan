/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage

# Filament republishes these on every composer install via the filament:upgrade hook,
# so committing them only creates conflicts and stale assets after a version bump.
/public/css/filament
/public/js/filament
/storage/*.key
/vendor
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
mysql
.bash_history

# macOS
.DS_Store
